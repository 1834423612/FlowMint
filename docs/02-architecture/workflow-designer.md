# Workflow Designer Notes

The visual editor should support growth without destructive rewrites.

## Primary zones

- left node palette
- center ReactFlow canvas
- right config and validation panel

## Core editor concerns

- node registry
- edge rules
- graph validation
- version snapshots
- node config schema rendering
- readable default layout
- future support for subflows and reusable blocks
